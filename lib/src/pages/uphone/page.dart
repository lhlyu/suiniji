// Flutter imports:
import 'package:flutter/material.dart';

class UPhonePage extends StatelessWidget {
  const UPhonePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Text("修改手机号码"),
      ),
    );
  }
}
